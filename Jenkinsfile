pipeline {
  agent any
  stages {
    stage('demo') {
      steps {
        echo "this is a demo"
        script {
           helloWorld([name: 'Newman', dayOfWeek: 'Friday']
        }
      }
    }
  }
}
